---
event_source:
  class: Honeybee\Infrastructure\DataAccess\Connector\GuzzleConnector
  settings:
    auth:
      username: ${config::couchdb::user}
      password: ${config::couchdb::password}
    host: ${config::couchdb::host}
    port: ${config::couchdb::port}
    transport: ${config::couchdb::transport}
    database: hlx-security
    status_test: /
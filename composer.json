{
    "name": "accordia/php-config",
    "type": "library",
    "description": "Config loader and provider for php.",
    "keywords": [ "config" ],
    "homepage": "https://github.com/accordia/php-config",
    "license": "MPL-2.0",
    "prefer-stable": true,
    "require": {
        "php": ">=7.1",
        "shrink0r/php-schema": "^0.3"
    },
    "require-dev": {
        "phpunit/phpunit": "^6.2",
        "squizlabs/php_codesniffer": "^3.0",
        "symfony/finder": "^3.3",
        "symfony/yaml": "^3.3",
        "accordia/data-structures": "master@dev",
        "satooshi/php-coveralls": "master@dev"
    },
    "autoload": {
        "psr-4": { "Accordia\\PhpConfig\\" : "src" }
    },
    "autoload-dev": {
        "psr-4": { "Accordia\\Tests\\PhpConfig\\": "tests" }
    },
    "scripts": {
        "build": [
            "@test",
            "@code-sniffer",
            "@validate-json"
        ],
        "test": "phpunit",
        "code-sniffer": "phpcs --extensions=php --standard=psr2 src tests",
        "code-sniffer-autofix": "phpcbf --standard=psr2 -d tabWidth=4 src",
        "validate-json": "composer validate --no-check-all --no-check-lock",
        "validate-deps": "composer validate --with-dependencies --strict"
    }
}
